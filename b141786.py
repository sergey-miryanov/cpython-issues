import random

rand = random.Random(38720)
import sys

def b():
    for c in range(23):
        rand.random()
    rand.random()
    if rand.random() > 0.1:
        rand.random()
    rand.random()
    rand.random()
    for c in range(4):
        rand.random()
    if rand.random():
        0
    if rand.random() > 0.1:
        rand.random()
    rand.random()
    if rand.random():
        rand.random()
    if rand.random():
        rand.random()
    rand.random()
    if rand.random():
        rand.random()
    rand.random()
    if rand.random():
        if rand.random() > 0.1:
            rand.random()
    if rand.random() < rand.random():
        ...
    if rand.random() < rand.random():
        ...
    if rand.random():
        ...
    if rand.random() > 0.1:
        rand.random()
    rand.random()
    for c in range(12):
        rand.random()

    def random(B):
        if rand.random() < 0.1:
            random(B)

    def Random(B):
        if rand.random():
            random(B)

    def b(B):
        if rand.random():
            Random(B)

    def c(B):
        if rand.random() < 0.1:
            b(B)

    def a(B):
        if rand.random() > 0.1:
            c(B)

    def A(B):
        if rand.random() > 0.1:
            a(B)

    def C(B):
        if rand.random() > 0.1:
            A(B)

    def B(B):
        if rand.random() < 0.1:
            C(B)

    for D in range(200):
        try:
            B(D)
        except:
            ...

    class A:
        def __del__(C):
            b = sys._getframe(1)
            exec("D=0", b.f_globals, b.f_locals)

    B = A()
    if rand.random() < 0.1:
        if rand.random() < 0.1:
            if rand.random() < 0.1:
                for D in range(D):
                    if rand.random() < 0.1:
                        if rand.random() > 0.1:
                            if rand.random():
                                if rand.random() > rand.random():
                                    C
                    if rand.random() < 0.1:
                        if rand.random() < 0.1:
                            if rand.random() < 0.1:
                                del B
                    if rand.random() < 0.1:
                        if rand.random() < 0.1:
                            if rand.random():
                                0
    if rand.random() > 0.1:
        if rand.random() > 0.1:
            rand.random()
    rand.random()
    if rand.random() > 0.1:
        rand.random()
    rand.random()
    if rand.random() > 0.1:
        if rand.random() > 0.1:
            rand.random()
    rand.random()
    rand.random()
    rand.random()
    if rand.random() > 0.1:
        if rand.random() > 0.1:
            rand.random()
            if rand.random() > rand.random():
                0
    if rand.random():
        C
    if rand.random() > 0.1:
        rand.random()
    rand.random()
    for c in range(3):
        if rand.random():
            0
    for c in range(2):
        rand.random()

for D in range(5064):
    b()
